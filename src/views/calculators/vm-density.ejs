<%- include('../partials/header.ejs', { title: 'VM Density Calculator' }) %>

<div class="row">
    <div class="col-12">
        <h1><i class="bi bi-server"></i> VM Density Calculator</h1>
        <p class="lead">Calculate how many virtual machines can run on a physical host based on CPU and memory resources.</p>
        
        <div class="card">
            <div class="card-header">
                <h3>Host Specifications</h3>
            </div>
            <div class="card-body">
                <form action="/calculators/vm-density" method="POST">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="physicalCores" class="form-label">Physical CPU Cores</label>
                                <input type="number" class="form-control" id="physicalCores" name="physicalCores" min="1" required>
                            </div>
                            
                            <div class="mb-3">
                                <label for="coreSpeed" class="form-label">Core Speed (GHz)</label>
                                <input type="number" class="form-control" id="coreSpeed" name="coreSpeed" step="0.1" min="0.1" required>
                            </div>
                        </div>
                        
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="memoryGB" class="form-label">Total Memory (GB)</label>
                                <input type="number" class="form-control" id="memoryGB" name="memoryGB" min="1" required>
                            </div>
                            
                            <div class="mb-3">
                                <label for="vmCores" class="form-label">CPU Cores per VM</label>
                                <input type="number" class="form-control" id="vmCores" name="vmCores" min="1" value="2" required>
                            </div>
                            
                            <div class="mb-3">
                                <label for="vmRamGB" class="form-label">RAM per VM (GB)</label>
                                <input type="number" class="form-control" id="vmRamGB" name="vmRamGB" min="1" value="4" required>
                            </div>
                        </div>
                    </div>
                    
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-calculator"></i> Calculate Density
                    </button>
                </form>
            </div>
        </div>
        
        <div class="card mt-4">
            <div class="card-header">
                <h3>Best Practices</h3>
            </div>
            <div class="card-body">
                <ul>
                    <li>Reserve 20-30% of resources for overhead and peak usage</li>
                    <li>Consider CPU and memory contention when determining final density</li>
                    <li>Account for overhead from hypervisor and other services</li>
                    <li>Reserve resources for maintenance operations</li>
                    <li>Monitor performance after deployment to optimize</li>
                </ul>
            </div>
        </div>
    </div>
</div>

<%- include('../partials/footer.ejs') %>