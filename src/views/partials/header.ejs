<!DOCTYPE html>
<html lang="<%= language || 'en' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - VMware Tool</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="bi bi-server"></i> VMware Tool
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/"><i class="bi bi-house-door"></i> <%= t('navigation.home') %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/resources"><i class="bi bi-cpu"></i> <%= t('navigation.resources') %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/calculators"><i class="bi bi-calculator"></i> <%= t('navigation.calculators') %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/glossary"><i class="bi bi-book"></i> <%= t('navigation.glossary') %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/knowledge"><i class="bi bi-journals"></i> <%= t('navigation.knowledge') %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/course"><i class="bi bi-book"></i> <%= t('navigation.course') %></a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="languageDropdown" role="button" data-bs-toggle="dropdown">
                            <i class="bi bi-globe"></i> <%= t(`language.${language || 'en'}`) %>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/lang/en?redirect=<%= encodeURIComponent(req.originalUrl || '/') %>"><%= t('language.en') %></a></li>
                            <li><a class="dropdown-item" href="/lang/vi?redirect=<%= encodeURIComponent(req.originalUrl || '/') %>"><%= t('language.vi') %></a></li>
                        </ul>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#"><i class="bi bi-person"></i> <%= t('navigation.login') %></a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container my-4">