<%- include('../partials/header.ejs', { title: 'Knowledge Base - Search Results' }) %>

<div class="container">
    <div class="row">
        <div class="col-12">
            <!-- Enhanced Search Results Header -->
            <div class="search-results-header text-center mb-4">
                <h1><i class="bi bi-search"></i> Knowledge Base Search Results</h1>
                <p class="lead">Found <%= results.length %> articles matching "<%= query %>"</p>
            </div>
            
            <div class="mb-4 text-center">
                <a href="/knowledge" class="btn btn-secondary">
                    <i class="bi bi-arrow-left"></i> Back to Knowledge Base
                </a>
            </div>
            
            <% if (results.length === 0) { %>
                <div class="alert alert-warning text-center" role="alert">
                    <h4><i class="bi bi-exclamation-triangle"></i> No Results Found</h4>
                    <p>No articles matched your search query. Try different keywords.</p>
                </div>
            <% } else { %>
                <div class="row">
                    <% results.forEach(article => { %>
                        <div class="col-lg-6 col-xl-4 mb-4">
                            <div class="card h-100 knowledge-search-result-card">
                                <div class="card-body d-flex flex-column">
                                    <h5 class="card-title"><%- article.title %></h5>
                                    <h6 class="card-subtitle mb-2 text-muted"><%- article.category %></h6>
                                    <div class="card-text flex-grow-1"><%- article.excerptHtml %></div>
                                    <a href="/knowledge/article/<%= article.id %>" class="btn btn-primary mt-auto">
                                        <i class="bi bi-book"></i> Read Article
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } %>
        </div>
    </div>
</div>

<style>
.knowledge-search-result-card {
    transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
}

.knowledge-search-result-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
}

.search-results-header h1 {
    font-weight: 700;
    color: #2c3e50;
}
</style>

<%- include('../partials/footer.ejs') %>